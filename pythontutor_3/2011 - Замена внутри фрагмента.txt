# Условие
# Дана строка. 
# Замение в этой строке все появления буквы h на букву H, кроме первого и последнего вхождения.

s = input()

b = s.find('h')
e = s.rfind('h')

print(s[:b+1] + s[b+1:e].replace('h', 'H') + s[e:])
